name: tv_tracker
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # Networking
  http: ^1.1.0

  # Persistent storage (save watched shows)
  shared_preferences: ^2.2.3

  # For SVG logos (streaming platforms, optional)
  flutter_svg: ^2.0.7

  # iOS-style icons
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # Add your SVG or image assets here if needed, e.g.:
    # - assets/logos/netflix.svg
    # - assets/logos/hbo.svg
